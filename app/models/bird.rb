class Bird < ApplicationRecord
end
#it makes sense to create a custom route when we can manipulate 
 #our data in the backend so that when we make a request
 #we don't have to send data when when incrememt our data